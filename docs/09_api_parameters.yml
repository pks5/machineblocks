# ============================================================================
# SCAD-facing parameter pointer — NOT the parameter inventory
# ============================================================================
#
# Sole SSOT for MachineBlocks native parameters (names, types, docs, defaults,
# scad:value) and type shapes:
#   domains/machineblocks/mbom/blocks/com/machineblocks/bml/core/NativeBlock.bml
#   domains/machineblocks/mbom/blocks/com/machineblocks/bml/type/*.bml
#
# This file previously duplicated the full parameter catalog for SCAD/AI use.
# That inventory has been removed to stop Markdown/YAML drift. Do not reintroduce
# per-parameter defaults/docs here — change NativeBlock / type BML instead.
#
# Optional future use: regenerate SCAD-only runtime notes from BML if a tool needs
# a machine-readable mirror. Until then this file is a pointer stub only.
#
# Conventions (XY / XYZ / AB): docs/04_types.md
# Parse / serialize commentary: docs/04_types.md · scad-lib/docs/05_types_and_mapping.md
# ============================================================================

version: "3.0.6"
status: stub-pointer
ssot:
  parameters: domains/machineblocks/mbom/blocks/com/machineblocks/bml/core/NativeBlock.bml
  types: domains/machineblocks/mbom/blocks/com/machineblocks/bml/type/
parameters: []   # intentionally empty — see NativeBlock.bml

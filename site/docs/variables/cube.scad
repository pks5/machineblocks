// Variable names can be chosen freely!

// Size of the Cube in the X-direction (width)
sizeX = 10;

// Size of the Cube in the Y-direction (depth)
sizeY = 10;

// Size of the Cube in the Z-direction (height)
sizeZ = 10;

// Create the Cube
// The variables are used instead of numbers
cube(size = [sizeX, sizeY, sizeZ], center = true);